[build-system]
requires = [
    "scikit-build-core",
    "nanobind"
]
build-backend = "scikit_build_core.build"

[project]
name = "vptree"
version = "1.0.0"

[tool.scikit-build]
cmake.build-type = "Release"